# Python Automation Scripts

A small collection of practical scripts that automate repetitive tasks.

## Setup (Windows)
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt